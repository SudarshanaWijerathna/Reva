.git
.gitignore
venv
.venv
__pycache__
*.pyc
*.pyo
*.pyd
.pytest_cache
.mypy_cache
.ruff_cache

# Frontend — not needed in the backend container
frontend/
node_modules

catboost_info
reports

# Raw datasets — model artifacts are in ml/ and are already included.
# Excluding data/ saves significant Cloud Build context upload time.
data/

*.log
.env
.env.*

# Oracle / compose deployment files (not needed inside the container)
docker-compose*.yml
docs/
